mixin information(header, type, img, text)
  section.information(class='information--' + type)
    h3.information__header #{header}
    - var src = "img/info__" + type;
    picture.information__img
      source(srcset = src + '--xs.webp' type='image/webp')
      img(src= src + '--xs.jpg')
    div.information__lead !{text}
    if (type == 'main')
      a.information__button.button(href="#") Подробнее
